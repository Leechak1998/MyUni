<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="messageAdapter"
            type="com.example.myuni.adapter.MessageAdapter" />
        
        <variable
            name="sendMes"
            type="com.example.myuni.ui.fragment.ChatFragment" />

    </data>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".ui.fragment.ChatFragment">

        <!-- TODO: Update blank fragment layout -->
        <ListView
            android:id="@+id/lv_chat"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:divider="#3BAAAAAA"
            android:background="#3BAAAAAA"
            app:adapter="@{messageAdapter}"/>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:layout_gravity="bottom"
            android:background="@color/black"
            android:padding="10dp">

            <EditText
                android:id="@+id/et_input"
                android:layout_width="0dp"
                android:layout_weight="8"
                android:layout_height="match_parent"
                android:background="@color/white"/>

            <Button
                android:id="@+id/btn_send"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:layout_marginLeft="10dp"
                android:background="@color/cardview_dark_background"
                android:onClick="@{() -> sendMes.onSubmit()}" />

        </LinearLayout>


    </FrameLayout>
</layout>